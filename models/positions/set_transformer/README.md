# models/positions/set_transformer

Runs históricos del clasificador de roles por Set Transformer.

Cada carpeta de timestamp contiene:
- `set_transformer_checkpoint.pt`
- `metrics.json`
- `training_history.csv`
- `split.json`

El modelo operativo actual ya no vive en esta carpeta, sino en `models/positions/20260427_002133/`.
